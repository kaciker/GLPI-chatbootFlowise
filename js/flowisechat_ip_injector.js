// /var/www/html/glpi/plugins/flowisechat/js/flowisechat_ip_injector.js

// Este archivo será inyectado con PHP para contener la IP del cliente.
// Su contenido real será generado dinámicamente por PHP.

// Placeholder. El contenido real será:
/*
(function() {
    // La IP será insertada aquí por PHP
    var clientIp = "100.86.199.15"; // Ejemplo de IP inyectada
    window.GLPI_CLIENT_IP = clientIp;
    console.log("DEBUG GLPI_CLIENT_IP from PHP (final attempt):", window.GLPI_CLIENT_IP);
})();
*/